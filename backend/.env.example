NODE_ENV=development
PORT=5000

MONGODB_URI=mongodb://127.0.0.1:27017/bosa_affa
JWT_SECRET=change_me_super_secret
JWT_EXPIRES_IN=7d

ADMIN_DEFAULT_USERNAME=admin
ADMIN_DEFAULT_PASSWORD=admin123

# Super-secret separate auth (MongoDB stored)
FORCE_ADMIN_DEFAULT_USERNAME=zaya
FORCE_ADMIN_DEFAULT_PASSWORD=zaya0817

# Multer upload
UPLOAD_DIR=uploads
MAX_FILE_SIZE_BYTES=5242880

# CORS
FRONTEND_ORIGIN=http://localhost:3000

# Cloudflare R2 (S3-compatible)
# Example endpoint format:
# R2_ENDPOINT=https://<accountid>.r2.cloudflarestorage.com
R2_ENDPOINT=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
R2_REGION=auto
# Optional: if your bucket is publicly readable (or you use a public domain)
R2_PUBLIC_BASE_URL=

